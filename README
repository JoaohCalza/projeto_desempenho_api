🚀 Tecnologias Utilizadas

Lumen (PHP) – API padrão do gateway

FastAPI (Python)

.NET WebAPI (C#)

PostgreSQL 15

NGINX (Gateway Reverso)

Docker & Docker Compose

🐳 Executando o projeto

Todo o ambiente (gateway, banco e APIs) é iniciado através do Docker Compose.

🔗 Rotas das APIs pelo Gateway
API padrão (Lumen – PHP)

http://localhost:8080/users

API FastAPI (Python)

http://localhost:8080/api_python/users

API .NET WebAPI (C#)

http://localhost:8080/api_csharp/users

📌 Operações Disponíveis (CRUD)

POST /users – Criar usuário

GET /users – Listar usuários

GET /users/{id} – Buscar usuário por ID

PUT /users/{id} – Atualizar usuário

DELETE /users/{id} – Remover usuário

Para usar FastAPI ou .NET, basta adicionar o prefixo:
• /api_python/users
• /api_csharp/users

📡 Exemplos de Requisições (curl)

Os exemplos abaixo utilizam a API padrão (Lumen).
Para testar nas outras APIs, basta adicionar o prefixo.

Criar Usuário (POST)
curl -X POST http://localhost:8080/users \
-H "Content-Type: application/json" \
-d '{
  "name": "João Henrique",
  "email": "joao@email.com",
  "user": "joaologin",
  "password": "123456"
}'

Listar Usuários (GET)
curl http://localhost:8080/users

Atualizar Usuário (PUT)
curl -X PUT http://localhost:8080/users/11 \
-H "Content-Type: application/json" \
-d '{
  "name": "Maria Silva Atualizada",
  "email": "maria_atualizada@email.com",
  "user": "marialogin",
  "password": "654321"
}'

Deletar Usuário (DELETE)
curl -X DELETE http://localhost:8080/users/11